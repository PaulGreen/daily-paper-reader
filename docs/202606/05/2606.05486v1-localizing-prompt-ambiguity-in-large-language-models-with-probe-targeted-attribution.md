---
title: Localizing Prompt Ambiguity in Large Language Models with Probe-Targeted Attribution
title_zh: 使用探针目标归因方法定位大语言模型中的提示模糊性
authors: "Govind Ramesh, Yao Dou, Wei Xu"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.05486v1"
tags: ["query:prompt-learn"]
score: 8.0
evidence: 提出PRIG方法，将隐式提示歧义归因到LLM提示中特定词元位置
tldr: 大型语言模型常因提示模糊而出错，但模糊性作为潜在属性难以用现有针对输出的归因方法定位。本文提出PRIG方法，通过线性探针区分清晰与模糊提示，并利用梯度将探针分数归因至输入token位置。在编程、数学等合成及人工数据集上，PRIG定位模糊片段效果显著优于基线，AUROC达0.840和0.891，且优于GPT-5.4。该工具确认了关键贡献：潜在提示属性可经中间表征而非输出层被有效定位。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05486v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 718, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05486v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 860, \"height\": 566, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05486v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1366, \"height\": 601, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05486v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1406, \"height\": 225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05486v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 969, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05486v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 943, \"height\": 182, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05486v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 971, \"height\": 420, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05486v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1061, \"height\": 359, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05486v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1375, \"height\": 1997, \"label\": \"Table\"}]"
motivation: 现有归因方法仅针对可观测输出，无法定位作为潜在属性的提示模糊性，导致难以修复提示缺陷。
method: PRIG训练线性探针区分清晰与模糊提示，再对探针logit进行梯度归因，识别残差流中早期token的模糊贡献。
result: PRIG在合成基准上AUROC达0.840，黄金集上达0.891，超过GPT-5.4，并展示出域外泛化能力。
conclusion: PRIG为定位提示模糊部分提供了实用工具，并证明潜在提示属性可通过中间表征定位，而非输出级归因。
---

## 摘要
提示模糊性是大语言模型常见的失效原因，但因其是提示的潜在属性而难以定位，而现有归因方法旨在解释可观测的输出，如logits或生成的标记。我们提出PRIG，一种梯度归因方法，使用探针logit将潜在模糊性归因到标记位置。具体而言，PRIG训练一个线性探针来区分清晰提示与模糊提示，并将探针分数归因于残差流中早先的标记表示。为了实现标记级评估，我们通过重写每个提示中的一个任务关键句子，构建了涵盖编程、数学和写作的合成模糊性数据集，并辅以人工编写的金标准基准。在此设置下，PRIG在定位模糊片段方面显著优于梯度归因基线，在合成基准数据集上取得0.840 AUROC，在金标准集上取得0.891 AUROC。它还在句子级模糊性识别上超越GPT-5.4，并在域外保持有用信号。这些结果确立了PRIG作为识别提示哪些部分模糊的实用工具。更广泛地说，它们表明潜在提示属性可以通过中间表示而非输出级归因来定位。

## Abstract
Prompt ambiguity is a common source of failure in large language models, but is difficult to localize because it is a latent property of the prompt, while existing attribution methods are designed to explain observable outputs such as logits or generated tokens. We introduce PRIG, a gradient attribution method that uses a probe logit to attribute latent ambiguity to token positions. Specifically, PRIG trains a linear probe to distinguish clear prompts from ambiguous prompts and attributes the probe score to earlier token representations in the residual stream. To enable token-level evaluation, we construct synthetic ambiguity datasets across coding, math, and writing by rewriting one task-critical sentence per prompt, and complement them with a human-written gold benchmark. In this setting, PRIG localizes ambiguous spans substantially better than gradient attribution baselines, achieving 0.840 AUROC on the combined synthetic benchmark and 0.891 AUROC on the gold set. It also outperforms GPT-5.4 on sentence-level ambiguity identification and retains useful signal out-of-domain. These results establish PRIG as a practical tool for identifying which parts of a prompt are ambiguous. More broadly, they suggest that latent prompt properties can be localized through intermediate representations, rather than through output-level attribution.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **问题背景**：大语言模型（LLMs）在使用时，如果输入的提示（prompt）存在模糊性，模型很容易产生错误或不符合预期的输出。然而，提示模糊性是一种隐含的、潜在的属性，难以直接定位到具体的词或句子。
- **现有方法局限**：传统的归因方法（如基于梯度的显著性图）设计初衷是解释可观测的输出（如 logits 或生成的 token），无法直接用于定位这种未在输出层直接表现出来的潜在属性。
- **本文动机**：迫切需要一种能够将“提示模糊性”这一潜在属性归因到输入提示中具体 token 位置的方法，以便开发者和用户能够识别并修正提示中的歧义部分，提升模型交互的可靠性。
- **整体含义**：本文不仅提出了一种实用的提示模糊性定位工具，更重要的是证明了：**潜在提示属性可以通过模型的中间表征（而非输出层）进行有效定位**，为理解和干预语言模型内部状态提供了新的范式。

### 2. 论文提出的方法论

- **方法名称**：PRIG（**Pr**obe-targeted attribution to localize prompt amb**ig**uity），即“探针目标归因定位提示模糊性”。
- **核心思想**：绕过模型最终输出，直接利用模型中间层表征训练一个线性探针（linear probe）来判断整条提示是否模糊，然后对该探针的输出分数进行反向梯度归因，从而将模糊性定位到输入 token 上。
- **关键技术细节**：
  - **线性探针训练**：
    - 从模型的某一中间层残差流中提取每个 token 的表示。
    - 对于每条提示，将其所有 token 的表示取平均（或使用最后一个 token 的表示），训练一个线性分类器（探针）来区分“清晰提示”与“模糊提示”。
    - 探针的输出可以看作对提示整体模糊程度的打分（探针 logit）。
  - **梯度归因**：
    - 对训练好的探针，计算其输出 logit 关于输入 token 嵌入（或残差流中早期 token 表示）的梯度。
    - 通过梯度的大小来归因每个 token 对模糊性分数的贡献，梯度绝对值（或梯度×输入）较大的 token 被认为是导致歧义的关键位置。
- **算法流程**（文字描述）：
  1. 准备包含成对的清晰与模糊提示的数据集（模糊版本通过改写任务关键句子生成）。
  2. 选定 LLM（如某开源模型），冻结模型参数，在某一中间层收集提示的 token 表示。
  3. 训练一个线性探针，输入为提示的表示向量，输出为二分类 logit（模糊 vs 清晰）。
  4. 对于待定位的提示，前向传播得到探针的模糊性 logit，然后反向传播计算该 logit 对输入层 token 的梯度。
  5. 按 token 梯度绝对值排序，高梯度 token 被标记为模糊性来源。

### 3. 实验设计

- **数据集/场景**：
  - **合成数据集**：覆盖编程（coding）、数学（math）、写作（writing）三个领域。通过系统性地重写每个提示中的一条“任务关键句子”来制造模糊版本，确保模糊性可被精确地定位到句子甚至 token 级别。
  - **人工金标准基准（Gold Benchmark）**：由人工撰写的高质量模糊-清晰提示对，用于评估方法的真实表现和泛化能力。
- **评估基准（Benchmark）**：
  - Token 级定位任务：判断被归因方法赋予高分的 token 是否与真实模糊片段重合，使用 AUROC（Area Under the Receiver Operating Characteristic curve）衡量定位的准确性。
  - 句子级识别任务：判断方法能否正确识别出包含歧义的句子，与 GPT-5.4 等强基线对比。
- **对比方法**：
  - 传统梯度归因基线（如基于模型输出 logits 或生成概率的梯度归因，例如对最终输出 token 的梯度、Saliency 等）。
  - GPT-5.4（作为黑盒语言模型的代表）用于句子级模糊性识别对比。

### 4. 资源与算力

- **文中未明确提及**使用了多少 GPU 型号、数量及训练时长。考虑到方法仅需在冻结的大模型上训练一个轻量线性探针并计算梯度，算力需求相对较低，但具体硬件配置和运行时间未被报告。

### 5. 实验数量与充分性

- **主要实验组数**：
  - 在合成数据集上的 token 级定位评估（三个领域合并及分领域）。
  - 在人工金标准数据集上的 token 级定位评估。
  - 句子级模糊性识别实验，与 GPT-5.4 对比。
  - 跨领域泛化实验（域外保持有用信号）。
  - 可能包含的消融实验（如不同中间层的选择、探针设计等，虽未在摘要中详述，但符合此类工作的常规）。
- **充分性与公平性分析**：
  - 实验覆盖了多领域合成数据和人工撰写数据，考虑了域内和域外测试，评估较为全面。
  - 对比了基于输出的梯度归因基线和强大的语言模型（GPT-5.4），比较对象合理。
  - 使用了标准定量指标（AUROC）进行评估，结果客观。整体实验设计能够支撑论文的主要主张。

### 6. 论文的主要结论与发现

- **方法有效性**：PRIG 在定位提示模糊性片段的 token 级任务上显著优于传统梯度归因基线，在合成基准上 AUROC 达 0.840，在人工金标准集上达 0.891。
- **超越黑盒模型**：在句子级模糊性识别任务中，PRIG 的表现超过了 GPT-5.4。
- **泛化能力**：PRIG 在未被训练过的领域（跨域）仍能保持有用的定位信号，显示一定的域外泛化性。
- **范式意义**：证明潜在提示属性（如模糊性）可通过模型的中间表征（探针 logit）进行定位，而不是依赖输出层归因，这为未来研究模型内部表征和可控性提供了新方向。

### 7. 优点

- **方法创新**：将线性探针与梯度归因巧妙结合，首次解决了“潜在提示属性”的定位问题，跳出了仅解释可观测输出的传统框架。
- **评估严谨**：构建了多领域合成数据集和人工金标准，使得 token 级别的定位评估成为可能，极大提高了评估的精确度和可信度。
- **实用性强**：PRIG 可直接用于实际提示工程，帮助用户快速定位并消除提示中的模糊来源，且不需要模型训练或微调（只需训练轻量探针）。
- **理论贡献**：揭示了中间表征携带可定位的潜在语义信息，为模型可解释性研究打开了新的窗口。

### 8. 不足与局限

- **算力与训练细节缺失**：文中未提供探针训练和梯度计算的具体资源消耗情况，不利于复现成本评估。
- **依赖参考提示对**：训练探针需要成对的清晰-模糊提示，获取高质量的人工标注数据成本较高；合成数据构建规则可能无法完全模拟真实世界模糊性的复杂多样性。
- **模糊性定义的局限性**：方法将模糊性视为二分类（清晰 vs 模糊），但实际中模糊性是连续的、多类型的，探针对模糊性的判别可能过于简化。
- **归因粒度**：梯度归因天然存在饱和、扩散等问题，token 级归因的精确边界可能受模型内部信息混合的影响，解释仍需谨慎。
- **跨模型泛化未验证**：实验可能主要基于单一 LLM（文中未指明多个模型），探针和归因在不同架构或规模模型之间的迁移性能未知。
- **下游任务验证欠缺**：仅评估了定位的准确性，未展示通过基于 PRIG 定位结果来修改提示后，模型表现的实际提升效果，缺少端到端的修复验证。

（完）
