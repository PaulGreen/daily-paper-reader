---
title: Supervision versus Demonstration-Based In-Context Learning for Multiword Expression Classification
title_zh: 监督与演示式上下文学习在多词表达分类中的对比
authors: "Sercan Karakaş, Yusuf Şimşek"
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.07479v1"
tags: ["query:prompt-learn"]
score: 9.0
evidence: 使用演示的上下文学习
tldr: 土耳其语习语轻动词结构（LVC）因表面形式与字面组合共享而难以自动检测。研究将其作为二分类任务，在匹配对照集上对比有监督BERTurk微调与三种指令微调LLM的零样本、单样本和少样本提示表现。零样本LLM对负样本高精度但LVC召回极低；单样本提示显著改善召回却引入模型特异性偏差；少样本提示使GPT-OSS-20B和Qwen 2.5-14B校准更优，达到稳健整体性能。结果揭示提示敏感性，精心设计的演示可让LLM匹配或超越有监督基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.07479v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 780, \"height\": 654, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07479v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 640, \"height\": 399, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07479v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 667, \"height\": 344, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.07479v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 792, \"height\": 268, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07479v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 728, \"height\": 173, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07479v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 677, \"height\": 189, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07479v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1319, \"height\": 524, \"label\": \"Table\"}]"
motivation: 土耳其语习语轻动词结构因形义歧义难以自动分类，需评估监督与提示学习范式的优劣。
method: 采用BERTurk微调模型与三种指令微调LLM，在零、单及少样本提示下进行LVC二分类实验，使用匹配对照集评估。
result: 零样本LLM召回极低；单样本提升但引入偏见；少样本下GPT-OSS-20B与Qwen2.5-14B校准优良，监督基线仍具竞争力。
conclusion: 提示对LLM性能影响显著，精心设计的演示可使其匹配或超越监督模型，为元语言分类提供新思路。
---

## 摘要
土耳其语惯用轻动词结构（LVCs）对多词表达处理具有挑战性，因为它们常与完全字面化的动宾组合共享表层形式，却作为单一、部分惯用的谓语发挥作用。我们将土耳其语LVC检测构建为二分类任务（字面义与惯用义），并在人工构建的对照集（N=147）上进行评估，该集合包含匹配的负例：域外随机句子和域内字面对照（NLVC），以及LVC正例。我们将监督式土耳其语编码器基线（带分类头的BERTurk）与三个来自不同家族的指令微调大语言模型在零样本、单样本和少样本提示下进行比较，并分析演示如何改变错误分布。在零样本条件下，大语言模型在负例上表现良好，但LVC召回率极低。单样本提示显著提升LVC检测，但会引入强烈的模型特定偏差，导致模型过度预测或不足预测LVC。更丰富的少样本提示改善了校准，使得GPT-OSS-20B和Qwen 2.5-14B取得了稳健的整体性能。总体而言，结果突显了土耳其语元语言分类中显著的提示敏感性：监督基线仍具竞争力，而通过精心构建的演示，提示后的大语言模型在LVC上可匹敌或超越之。

## Abstract
Turkish idiomatic light verb constructions (LVCs) are challenging for multiword expression processing because they often share the same surface form as fully literal verb-object combinations while functioning as a single, partially idiomatic predicate. We frame Turkish LVC detection as a binary classification task (literal meaning vs. idiomatic meaning) and evaluate on a manually created controlled set (N=147) with matched negatives: out-of-domain random sentences and in-domain literal controls (NLVC), alongside LVC positives. We compare a supervised Turkish encoder baseline (BERTurk with a classifier head) to three instruction-tuned LLMs from different families under zero-shot, one-shot, and few-shot prompting, and analyze how demonstrations shift error profiles. In zero-shot, LLMs perform well on negatives but show very low LVC recall. One-shot prompting sharply improves LVC detection but can induce strong, model-specific biases, leading models to overpredict or underpredict LVCs. A richer few-shot prompt improves calibration and yields robust overall performance for GPT-OSS-20B and Qwen 2.5-14B. Overall, the results highlight substantial prompt sensitivity in Turkish metalinguistic classification: the supervised baseline remains competitive, while prompted LLMs can match or exceed it on LVCs with carefully constructed demonstrations.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：土耳其语中存在大量惯用轻动词结构（LVC），其表面形式与完全字面的动宾组合相同，但功能上是一个部分惯用的谓语整体，这给自然语言处理的语义理解带来了挑战。模型能否以及如何正确区分LVC的字面义与惯用义，是提升土耳其语多词表达处理能力的关键。
- **整体含义**：论文旨在系统比较纯监督式分类基线（BERTurk）与基于提示的指令微调大语言模型（三种不同家族）在LVC二分类任务上的表现差异，尤其考察零样本、单样本和少样本三种在上下文学习范式下模型的敏感性与错误模式变化，为元语言分类任务的方法选型提供依据。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将LVC检测形式化为二元分类任务（[1]=惯用义LVC，[0]=字面义或非LVC），在人工构建的严格对照集上，通过控制提示中示例的数量和内容，揭示不同规模、不同家族的指令微调大语言模型的决策边界和校准特性。
- **模型实现**：
    - **监督式基线**：使用BERTurk（32K和128K两个词表版），在通用依存树库（UD Turkish系列）中提取并标注LVC/非LVC句子，对BERT模型加二分类头进行微调。从82,319句标注数据中训练，并采用80/20分层划分、早停等方式避免过拟合。
    - **提示式大语言模型**：选取Llama 3.1 8B、GPT-OSS 20B、Qwen 2.5 14B三种模型，通过Ollama本地部署，使用温度0.1进行解码，要求输出仅包含[0]或[1]。
    - **提示构造**：
        - 零样本：仅给出任务指令和标签定义。
        - 单样本：指令后附加一个LVC正例和一个字面负例（按目标动词模板给出）。
        - 少样本：指令后提供多个动词模板以及对应的正负例对（共多对示例），促使模型学习更宽泛的正负变异。
- **数据分析**：采用卡方检验和事后比例检验评估模型间及条件间差异，并报告精度、召回率以及整体成功率和条件成功率。

### 3. 实验设计：数据集、基准线、对比方法

- **数据集**：
    - **评价集**：手工构造的147句土耳其语诊断集，分为三个条件各49句：
        - LVC（正例）：惯用轻动词结构。
        - NLVC（负例，字面域内对照）：使用与LVC相同动词的字面义句子。
        - Random（负例，随机域外对照）：与测试动词无关的随机正常句子。
    - **模型训练监督数据**：UD土耳其树库9个子库合并后，经自动抽取与人工筛选，得到82,319句（9,491个LVC实例，涵盖compound:lvc和noun-verb compound两种模式），用于BERTurk微调。
- **基准线**：
    - 两个BERTurk变体（32K和128K）作为监督式分类基线。
- **对比方法**：
    - 三种不同家族的指令微调大语言模型在零样本、单样本、少样本三种提示设置下的性能。
    - 模型间的综合对比以及各条件下错误类型（假阳性FP/假阴性FN）分析。

### 4. 资源与算力

- 论文未详细说明训练BERTurk所需的GPU类型、数量及训练时长，仅给出学习率（2e-5）、批次大小（32）、epoch上限（10）、早停等超参数。
- 大语言模型推理通过本地Ollama运行，未报告具体硬件配置，只列出模型架构、参数量（8B、20B、14B）及量化格式（Q4_K_M或MXFP4）。
- 整体而言，算力需求细节缺失，无法评估计算开销。

### 5. 实验数量与充分性

- **主要实验数量**：
    - 实验1：零样本设置，在3个大语言模型和2个BERTurk基线上分别测试三个条件各49句，共5组对比。
    - 实验2：单样本提示设置，仅3个大语言模型，同样三条件输出，共3组对比。
    - 实验3：少样本提示设置，3个大语言模型，同样三条件，共3组对比。
    - 另外，整合了错误计数和总体成功率的对比表格（Table 4），并进行模型×条件、模型×提示体制的统计检验。
- **充分性与公平性**：
    - 对比充分，覆盖零/单/少样本和不同模型家族，观察值虽小但条件控制严格（同一动词的配对示例），统计检验负责任地避免了小型样本的假阳性。
    - 监督式模型与提示模型属于不同性质的学习范式（参数更新 vs. 无梯度），论文对此局限性予以明确说明，并强调实验重点在于提示敏感性而非绝对能力排序，实现了公平的解释框架。

### 6. 论文的主要结论与发现

- **零样本缺陷**：大语言模型在零样本下高度保守，对负例准确率极高（>90%），但LVC召回率崩溃式下降（GPT-OSS仅6.1%，Llama 3.1甚至为0%），导致整体精度产生误导性“还行”的假象。
- **单样本引发偏差**：加入一对正负例后，模型行为急剧变化，但出现模型特异性偏差：Llama 3.1过度预测正例（高LVC召回，但负例准确率骤降至28.6%），而Qwen 2.5仍倾向保守（高负例准确，低LVC召回），GPT-OSS相对平衡。
- **少样本改善校准**：更多示例提升了GPT-OSS和Qwen的整体性能（84~86%），且二者在正例上的表现趋于收敛；Llama 3.1正例依然强但负例假阳性仍多。少样本降低了单样本的极端失败模式。
- **监督基线竞争力**：BERTurk-128K在零样本对比中大幅优于未提示LLM（LVC准确率79.6% vs. 6.1%），在少样本提示下与GPT-OSS相比总体持平（84.4% vs. 84.4%），且更平衡，体现出任务特定监督在形态丰富语言中的价值。
- **提示体制调控错误模式**：演示主要通过重加权决策边界影响校准，而非简单提升理解能力；提示选择、数量和内容强烈决定最终错误类型。

### 7. 优点

- **严谨的任务构建**：使用同一动词的惯用/字面最小对照对，排除了简单动词词法启发式的干扰，直击模型深层的语义组合性判断。
- **多维度、多条件评估**：分三个独立条件（随机负、字面负、LVC正）分别报告，揭示了整体准确率掩藏的严重召回失败或偏向性错误，评估体系更具洞察力。
- **统计方法规范**：对小样本采用Holm校正后的卡方检验和两比例检验，避免夸大差异，增添了结论的可信度。
- **开源与可复现性**：公开代码、提示模板和评估材料，有利于后续验证与改进。
- **对比视角清晰**：明确监督与提示两种范式的本质差异（任务拟合 vs. 元语言推断），并指出LLM在具名示例后能逼近监督模型的潜力与条件。

### 8. 不足与局限

- **样本规模与泛化性**：评价集仅147句，且为人工构造的对照集，虽适合诊断性测试，但无法代表真实分布中的LVC多样性，结论可能局限。
- **提示模板存在变异性**：少样本提示中示例的选择、顺序可能大幅影响性能，论文未对提示的多种变体进行消融，结果可能对提示细节较敏感。
- **大语言模型固定配置**：仅测试了特定家族的特定规模模型，且解码温度固定（0.1），未探索不同系数或采样策略的影响。
- **计算资源未披露**：无法确认硬件条件对可复现性的影响。
- **监督基线不完美**：BERTurk的训练标签来自UD树库的自动+人工筛选，可能存在遗漏LVC或噪音，使对比并非在完全干净监督下进行。
- **跨语言普适性**：仅聚焦土耳其语，无法直接推广到其他形态丰富或轻动词现象差异大的语言。

（完）
