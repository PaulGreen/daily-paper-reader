---
title: "Fine-tuning Multi-modal LLMs with ART: Art-based Reinforcement Training"
title_zh: 使用ART微调多模态大语言模型：基于艺术的强化训练
authors: "Michal Chudoba, Sergey Alyaev, Petra Galuscakova, Tomasz Wiktorski"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.11854v1"
tags: ["query:prompt-learn"]
score: 9.0
evidence: 提出视觉输入优化作为软提示的替代方案，采用梯度调优
tldr: 参数高效微调技术如低秩适应（LoRA）和软提示虽显著降低微调成本，但它们需修改预编译、预优化的大语言模型计算图，导致高吞吐引擎（如vLLM）不完全支持，限制了实际部署。本文提出基于艺术的强化训练（ART），创新性地仅优化多模态大语言模型（MLLM）的原始视觉输入，通过将梯度反向传播至像素数组，在冻结的预编译计算图上实现软标记式微调，回避了图修改问题。在开源Qwen架构的不同模型尺寸及多个文本基准上验证，ART在数学推理和结构化工具使用等任务中达到与LoRA竞争性的准确率。更进一步，优化后的视觉输入可风格化为具有任务相关性的计算艺术作品，赋予微调过程可视化与艺术表达潜力，为高效、非侵入式模型适应提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11854v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1629, \"height\": 578, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11854v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1662, \"height\": 345, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11854v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1711, \"height\": 1029, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11854v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1713, \"height\": 370, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11854v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1552, \"height\": 240, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11854v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1115, \"height\": 320, \"label\": \"Table\"}]"
motivation: 现有PEFT方法需修改预编译计算图，导致高吞吐推理引擎vLLM等不支持，迫切需要一种无需改图的微调方法。
method: 仅优化多模态大模型的原始视觉输入，通过梯度反传至像素数组，在冻结预编译计算图上实现软标记微调，无需修改计算图。
result: 在不同尺寸的Qwen模型和多个文本基准上，ART在数学推理和结构化工具使用任务中取得与LoRA相当的准确率。
conclusion: ART通过视觉输入优化实现非侵入式微调，支持预编译图，并可生成任务驱动型艺术作品，为高效多模态微调提供新范式。
---

## 摘要
对于大语言模型（LLM）有两种主要的参数高效微调（PEFT）技术。低秩适应（LoRA）在LLM层之间引入额外的权重，而软提示则在LLM输入中引入额外的微调特定原始标记。然而，两者都需要修改预编译、预优化LLM的计算图。因此，两者都无法在像vLLM这样的高吞吐量引擎中得到完全支持。我们提出了使用ART（基于艺术的强化训练）进行微调的方法。该方法通过仅优化冻结的多模态大语言模型（MLLM）的原始视觉输入来注入信息，从而在预编译的计算图上实现软标记方法。它依赖于将梯度反向传播到普通像素阵列，因此支持任何微调目标。此外，优化后的视觉输入可以被风格化为与任务相关的计算艺术作品。该方法的有效性在流行的开源Qwen架构的不同规模和几个文本基准测试中得到了确认。具体而言，ART在数学和结构化工具使用基准测试中达到了与LoRA相竞争的准确性。

## Abstract
There are two main Parameter-Efficient Fine-Tuning (PEFT) techniques for Large Language Models (LLMs). While Low-Rank Adaptation (LoRA) introduces additional weights between the LLM layers, Soft Prompting introduces additional fine-tuning-specific raw tokens to an LLM input. However, both require modification to the computational graphs of precompiled, preoptimized LLMs. As a result, neither is fully supported in high-throughput engines like vLLM. We propose fine-tuning with ART (Art-based Reinforcement Training). The method injects information into a frozen Multimodal Large Language Model (MLLM) by optimizing only its raw visual input, thus enabling the soft-token approach on pre-compiled computational graphs. It relies on backpropagation of gradients back into a plain pixel array and thus supports any fine-tuning objective. Moreover, the optimized visual input can be stylized as task-relevant computational artworks. The approach's effectiveness is confirmed for different sizes of a popular open Qwen architecture and for several textual benchmarks. Specifically, ART reaches accuracy competitive with LoRA across mathematics and structured-tool-use benchmarks.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义
- **研究动机**：现有参数高效微调技术，如低秩适应和软提示，虽能降低微调成本，但都需修改预编译、预优化的模型计算图。这在高吞吐推理引擎中引发严重工程摩擦，如 vLLM 的动态多适配器加载会破坏 CUDA 图、降低吞吐。
- **核心问题**：如何在不修改模型权重或计算图的前提下，实现对冻结多模态大语言模型的非侵入式任务适配，同时保持与高吞吐生产环境的完全兼容。
- **整体含义**：提出“基于艺术的强化训练”，将多模态模型的视觉输入通道重新定位为任务适应的接口。通过仅优化原始输入图像，在冻结的预编译计算图上实现软提示效果，既规避了架构限制，又赋予微调过程可视化与艺术表达潜力。

### 方法论
- **核心思想**：将可训练参数从模型权重（θ）转移至输入图像（X_pixel）。冻结整个多模态模型，仅对输入图像的像素进行梯度优化，使优化后的图像作为“软提示”引导模型在文本任务上的行为。
- **像素空间参数化**：
    - 为保证像素值在有效范围并支持无约束优化，在 logit 空间初始化可训练参数 X_raw。
    - 通过 sigmoid 变换并量化，将 X_raw 映射为标准的 8-bit RGB 图像 X_pixel，用于模型前向传播。
    - 反向传播时，将连续的 sigmoid 输出替换进冻结模型进行梯度计算，以确保数值稳定性。
- **奖励驱动的两轮优化循环**：
    - **轮次A（采样与优势估计）**：将量化后的图像与文本查询 Qi 输入给高吞吐引擎（vLLM）生成 N 个补全；用任务特定奖励评分，并基于群体相对策略优化的“无评论家”方式计算优势函数：
        > A_{i,j} = (R(o_{i,j}, y_i) - R̄_i) / (std(R_i) + ε_eps)
    - **轮次B（策略裁剪与反向传播）**：将连续图像张量输入冻结的模型副本，使用 DAPO 目标函数进行基于 PPO 风格裁剪的优化：
        > L(X_raw) = - \frac{1}{\sum |o_{i,j}|} \sum \min\left(r_{i,j,t} A_{i,j}, \text{clip}(r_{i,j,t}, 1-\epsilon_{low}, 1+\epsilon_{high}) A_{i,j}\right)
    - 最终输出是一个可部署的标准 PNG 图像文件，即经过优化的“ART 制品”。

### 实验设计
- **模型与架构**：在 Qwen3.5-0.8B 和 Qwen3.5-2B 两个模型规模上进行实验，使用单张 NVIDIA A100 GPU。
- **评估基准**：
    - **GSM8K**：小学数学应用题，评估多步算术推理能力。
    - **GPQA**：研究生级科学多选题，评估高精度科学推理能力。
    - **ToolMind**：结构化 API 工具调用，评估根据环境参数生成正确功能调用和参数填充的能力。
- **对比方法**：
    - **非微调基线**：纯文本基线、随机图像、随机文本字符串、固定种子图像（零样本控制）。
    - **微调方法**：标准语言模型 LoRA 和本方法提出的优化图像。

### 资源与算力
- **硬件**：实验在 **单张 NVIDIA A100 GPU** 上进行。
- **训练时间**：
    - Qwen3.5-0.8B 上，ART 训练耗时远低于 LoRA：例如 GSM8K 训练，ART 耗时 1093.6 秒，而 LoRA 耗时为 2008.1 秒；ToolMind 训练，ART 耗时 1158.6 秒，LoRA 为 3797.4 秒。
- **配置细节**：训练步数为 100 步，组大小为 8，有效批次大小为 32。然而，论文未明确说明使用的 A100 显存版本（如 40GB 或 80GB）或具体数量。

### 实验数量与充分性
- **实验组合**：覆盖了 2 个模型规模 × 5 种方法 × 3 个基准 的多维度对比，构成了一个相对密集的实验网格。
- **客观公平性**：
    - 奖励函数和训练环境（如 DAPO 损失、组大小、步数）在 ART 和 LoRA 间严格对齐，确保了对比的公平性。
    - 非微调基线去耦合了图像存在、文本长度干扰等因素，实验设计严谨。
    - 对评估指标提供了基于 Bootstrap 的置信区间，结果可对比性强。
- **充分性**：实验在一定规模上验证了有效性，但仅限于 Qwen 系列和三个特定任务，覆盖面仍属有限，但内部设计严谨。

### 主要结论与发现
1.  **视觉前缀的独立增益**：即使不经过优化，仅仅在输入前添加一张随机或固定图像，也能为小型多模态模型带来巨大的性能提升（如 0.8B 模型在 GSM8K 上从 39.65% 提升至 54.59%），而添加等量随机文本则会严重损害性能。
2.  **ART 的竞争性甚至超越性**：
    - 在 0.8B 模型上，ART 在所有三个基准上全面超越所有非微调基线和 LoRA。
    - 特别是，ART 在 GSM8K (58.53%) 和 ToolMind (73.80%) 上大幅领先 LoRA (49.51% 和 69.50%)，证明了在极小型模型中，视觉通道是比模型权重更有效的任务信号注入点。
3.  **高精推理任务局限**：在需要高精度推理的 GPQA 基准上，添加任何视觉前缀（包括 ART）都会轻微损害性能，而 LoRA 表现更优，暗示了方法在抽象推理任务上的潜在局限。但差异因数据集样本量小（273条）而不具有统计决定性。
4.  **ART 的存储机制与效率**：
    - **信息存储**：优化后的图像文件大小（PNG 压缩后）显著增长，表明梯度优化将任务信息以高频“噪音”形式直接编码进图像像素中，形成一种“AI隐写术”。且该信息在 32 位到 8 位的量化过程中得以鲁棒保留。
    - **训练与推理高效**：ART 的训练和推理速度均显著快于 LoRA，因其无需加载适配器权重，模型保持完全冻结。

### 优点
- **极强的生产友好性**：完全非侵入式。部署时仅需将一个图像文件与请求一同发送，无需任何特殊的权重管理、内核或引擎修改，完美兼容 vLLM 等预编译引擎。
- **方法新颖且高效**：创造性地将多模态模型的视觉通道用作通用软提示接口，训练和推理效率优于 LoRA。
- **实验设计严谨**：通过随机文本、随机图像与固定种子图像等精心设计的对照实验，清晰地解耦并证明了视觉前缀的独立作用和 ART 优化的增益。

### 不足与局限
- **架构单一性验证**：所有实验仅在 Qwen3.5 架构上完成，其跨架构（如 LLaVA, InternVL）的通用性未得到验证。
- **任务覆盖面窄**：仅在三个特定基准上做了验证，且实验结果揭示该方法在 GPQA 这类抽象、高精度推理任务上效果不佳，其适用任务边界需要更广泛的探索。
- **信息容量解释不足**：虽然将 PNG 文件大小的增长作为信息存储的代理指标，但这更多是一个现象，其与存储的特征信息量、与微调性能之间的直接定量关系未得到深入分析。
- **算力描述不完整**：未明确说明所用 GPU 的显存版本或数量，以及训练的总 GPU 小时数。

（完）
