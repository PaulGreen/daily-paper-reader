---
title: "PromptMN: Pseudo Prompting Language"
title_zh: PromptMN：伪提示语言
authors: Enkhzol Dovdon
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.17164v1"
tags: ["query:prompt-learn"]
score: 8.0
evidence: 提出一种伪提示领域特定语言，用类型化指令标注自然语言以减少歧义
tldr: "当前，自然语言提示因隐含假设和上下文歧义常导致代理工作流失败，尤其影响软件开发中的多步骤协作。为此，提出PromptMN，一种伪提示领域特定语言，通过%前缀类型化指令显式定义角色、目标、需求、优先、约束、计划、输入和输出，支持语义解析无需顺序。在Claude Opus、Gemini Pro、GPT-5.5等前沿模型上测试，模型能正确解析条件、重复、方法调用等复杂指令，无需微调，并展示了在软件开发生命周期各阶段的复用性。这些早期结果表明PromptMN是向更清晰、可审查的人机交互迈出的实用一步，有利于对齐人与AI工具。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17164v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1702, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17164v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1628, \"height\": 954, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17164v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1636, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17164v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1631, \"height\": 613, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17164v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1734, \"height\": 1025, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17164v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1693, \"height\": 1913, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17164v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1773, \"height\": 871, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17164v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1413, \"height\": 458, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17164v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1389, \"height\": 340, \"label\": \"Table\"}]"
motivation: 自然语言提示的隐式表达和上下文模糊性导致代理工作流频繁失败，亟需结构化但轻量的提示规范。
method: "设计PromptMN伪提示语言，利用%前缀的类型化指令显式标注自然语言中的角色、目标、约束等，实现有序或无序的语义解析。"
result: 前沿模型如Claude Opus 4.8、Gemini 3.1 Pro、GPT-5.5无需微调即可正确解析条件、循环等复杂结构，并适用于代码库新建、维护等SDLC场景。
conclusion: PromptMN为人类与AI交互提供了清晰、可审查且可重用的提示规范，需进一步大规模验证，但早期结果颇具前景。
---

## 摘要
提示已成为人类与生成式AI之间的主要交互方式，然而许多自然语言提示仍然十分脆弱：角色、目标、约束和预期输出往往埋没在散文中或被隐含。在智能体和软件开发工作流中，第一次交接时的误读可能传播至每一步，因为相当一部分智能体失败源于上下文歧义而非模型局限。本文引入PromptMN，一种伪提示领域特定语言，通过紧凑的、以%为前缀的类型指令标注自然语言，涵盖角色、目标、需求、优先级、约束、计划、输入和输出。语义解析允许作者以任意顺序编写，而模型按功能解释指令。PromptMN介于非正式提示和编程风格伪代码之间：结构化到足以被审视和复用，又轻量到能被分析师、管理者、开发者和整个软件开发生命周期（SDLC）中的利益相关者所使用。PromptMN还与逆向提示工程配合使用。要求模型将期望结果重述为PromptMN，使用户在行动之前检查推断的角色、目标、约束和缺失的假设，从而减少修复周期，并生成一个可复用的人工与AI工具对齐制品。PromptMN的可行性在多个前沿模型上进行了评估，包括Claude Fable 5、Claude Opus 4.8、Gemini 3.1 Pro和GPT-5.5。模型正确解析了PromptMN指令，包括重复、条件、方法和质数检查任务等复杂结构，无需微调。在给出的SDLC场景中，同一词汇可应用于新代码库、维护和重新设计。虽然大规模验证仍有待未来工作，但这些早期结果表明，PromptMN是迈向更清晰、更可审查的人与AI交互的务实一步。

## Abstract
Prompting has become the primary interface between humans and generative AI, yet many natural language prompts remain fragile: roles, goals, constraints, and expected outputs are often buried in prose or left implicit. In agentic and software development workflows, a misread at the first handoff can propagate through every step, since a significant portion of agent failures stem from context ambiguities rather than model limitations. This paper introduces PromptMN, a pseudo-prompting domain-specific language that annotates natural language with compact, %-prefixed typed directives covering roles, goals, requirements, priorities, constraints, plans, inputs, and outputs. Semantic resolution lets authors write in any order while the model interprets directives by function. PromptMN sits between informal prompting and programming-style pseudocode: structured enough to be inspectable and reusable, yet lightweight enough for analysts, managers, developers, and stakeholders across the software development lifecycle (SDLC). PromptMN also pairs with reverse prompt engineering. Asking a model to restate a desired outcome as PromptMN lets users inspect the inferred roles, goals, constraints, and missing assumptions before acting, reducing repair cycles and yielding a reusable artifact for aligning people and AI tools. PromptMN's feasibility is evaluated across several frontier models, including Claude Fable 5, Claude Opus 4.8, Gemini 3.1 Pro, and GPT-5.5. The models correctly resolved PromptMN instructions, including complex structures such as repetition, conditionals, methods, and a prime-checking task, without fine-tuning. The same vocabulary applies across new codebases, maintenance, and redesign in the SDLC scenarios presented. While large-scale validation remains future work, these early results suggest PromptMN is a practical step toward clearer, more reviewable human-to-AI interaction.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机与背景）

- **当前提示的脆弱性**：尽管提示工程已成为人类与生成式AI交互的主要手段，但自然语言提示中角色、目标、约束、预期输出等关键信息往往“淹没”在冗长的散文中或完全隐含。在智能体（agent）流水线和软件开发场景下，这种模糊性会在第一步传递时就被误读，并随着任务链不断放大，导致大量失败并非由于模型能力不足，而是源于上下文歧义。
- **结构化但非代码化的缺失**：现有实践要么停留在非正式的建议层面（如“请明确角色”），要么走向需要专业编程能力的伪代码（如 Python 风格的结构化提示）。中间缺乏一种既能被审查、复用，又不要求用户是程序员的轻量级、类型化的标注形式。
- **目标**：论文希望提供一种“伪提示领域特定语言（DSL）”——PromptMN，在不取代自然语言的前提下，通过少量可记忆的类型指令增强提示的可读性、可审查性与可再现性，从而使人机交互更可靠、更透明。

### 2. 论文提出的方法论

- **核心思想**：
  - 使用 `%` 前缀的紧凑保留关键词（如 `%role`、`%goal`、`%req`、`%if`、`%repeat`、`%out` 等），在普通自然语言中对关键意图元素进行类型标记。
  - 执行时进行**语义解析而非按源码顺序执行**，允许作者以任意顺序编写，解释器在执行前按功能重新排序。

- **语言结构与簇架构（共 7 个簇）**：
  - **簇 1：词法语法基础**（分界符 `∞ … ∞`、分号、花括号块、`<condition>` 占位符等）。
  - **簇 2：上下文与意图**（`%role`、`%goal`、`%problem`、`%domain` 等）确立人物、目标、领域背景。
  - **簇 3：需求与治理**（`%req`、`%should`、`%could`、`%mustnot` 等）引入 MoSCoW 式优先级和硬性约束，使约束显式化。
  - **簇 4：计划与编排**（`%plan`、`%<number>` 步骤、`%showplan`、`%trace`）显式拆分任务步骤，并强制在行动前出示计划、行动后输出推理轨迹。
  - **簇 5：控制流与计算**（`%if`、`%else`、`%repeat`、`%var`、`%method`、`%return` 等）将分支、循环、变量、方法抽象等带进提示层。
  - **簇 6：数据接口**（`%in`、`%data`、`%ignore`、`%out`、`%visualize`、`%diagram`）明确输入/输出数据边界并支持结构化数据传递，且能强制输出为视觉图。
  - **簇 7：元信息与生命周期**（`%add`、`%del`、`%update`、`%newconcept`、`%format`、`%meta`）支持在会话中扩充词汇、赋予版本/作者等元数据，并控制输出格式。

- **解释器契约**：不绑定特定模型或引擎，仅要求上下文窗口内包含 PromptMN 规范，或模型经过微调。兼容性可通过 `%showplan` 和 `%trace` 的输出进行检查，且语言本身支持 `%newconcept` 扩展，可随模型能力演进。

### 3. 实验设计（场景与评估）

- **评估模型**：在 Claude Fable 5、Claude Opus 4.8、Gemini 3.1 Pro、GPT‑5.5 四个前沿模型上进行零样本（无微调）测试。
- **评估形式**：没有使用传统 NLP 基准数据集，而是基于**任务场景演示**，分为两大类：
  - **结构化执行示例**：
    - 基础循环：`%repeat` 输出固定字符串。
    - 方法定义与调用：定义 `%is-prime` 方法（包含变量、条件、循环、返回），测试 `is-prime(23)`，所有模型均返回 `true`。
    - 条件分支链（Snake 游戏单帧决策）：对下一个格子是障碍/食物/空地的三路分支，验证模型能正确展示执行计划并生成游戏状态。
    - **五个 SDLC 场景**：（a）新建代码库——脚手架 Next.js 项目；（b）更新现有代码库——支付端点的幂等重试；（c）架构重设计——从单体改为事件驱动微服务；（d）需求规格（SRS）表达为 PromptMN 提示；（e）混合使用——在自然语言 PR 审查中附加少量指令如 `%aware`、`%mustnot`、`%showplan`。
  - **逆向提示工程**：给定一个期望的游戏创意，分别让模型生成：（a）PromptMN 格式的提示；（b）简单自然语言提示；（c）SRS（软件需求规格书）。通过对比展示 PromptMN 生成的结构更清晰、更适合人-AI 协作、且能直接暴露角色、目标、约束等。

### 4. 资源与算力

- **文中未明确提及任何计算资源**（无 GPU 型号、数量、训练时长）。所有实验均为对已训练模型的 **零样本推理测试**，未涉及微调，因此不产生额外算力消耗。

### 5. 实验数量与充分性

- 共包含 **约 7–10 个演示性案例**（基础控制流 3 例，SDLC 场景 4 大类共 5 个小节，逆向提示 3 个对照版本）。这些案例覆盖了从单指令输出到完整游戏逻辑、代码库操作、需求文档生成的跨度。
- 实验设计**偏向定性演示**，缺乏定量评估（如成功率、精确匹配率、人评分数），也没有系统性地与纯自然语言提示或伪代码提示进行性能比较。作者也明确指出“大规模验证有待未来工作”，因此**尚不充分证明 PromptMN 在复杂多样实际任务中的稳健性**。

### 6. 主要结论与发现

- **模型能够解析并遵循 PromptMN 的零样本指令**：四个前沿模型均能正确处理条件、循环、方法调用、步骤排序和计划输出，未出现直接失败。
- **单一词汇可覆盖多种 SDLC 阶段**：同一套指令在新建、维护、重新设计、需求文档生成和混合自然语言场景中均能有效表达任务。
- **逆向提示工程收益显著**：用 PromptMN 重构的提示比简单自然语言提示更结构化，比 SRS 更适于直接驱动 AI 编码，且能通过检查缺失假设来减少返工。
- **可审查性提升**：`%showplan` 和 `%trace` 等机制在任务执行前后植入可审计的控制点，降低链式错误传递的风险。
- 总体而言，PromptMN 被认为是“在自由格式自然语言与正式编程之间”的一个实用通信层，能将意图显式化并提升人-AI 协作的可信度。

### 7. 优点

- **轻量且低门槛**：48 个 mnemonic 关键词，无需编程背景即可使用，同时为开发者保留结构化能力。
- **语义解析与位置无关**：作者可按任意顺序编写，解释器根据功能重新组装，减轻提示组织的负担。
- **显式的约束与治理**：`%mustnot`、`%reqfunc`/`%reqnonfunc`、`%should`/`%could` 将 MoSCoW 优先级和硬约束直接变为可检查的第一类元素。
- **内置 review 点**：`%showplan` 和 `%trace` 在任务前后提供可审计的中间产物，便于人工或自动化校验。
- **兼容性与扩展性**：通过 `%newconcept` 实现领域词汇扩展，`%meta` 提供版本和目标运行时标记，语言可随模型进步而演化。
- **不依赖微调**：测试的所有模型仅需规范放入上下文即可执行，降低了部署成本。
- **与逆向工程天然结合**：逆向生成的 PromptMN 可直接作为清晰、可复用的意图制品，用于团队间或不同智能体之间的交接。

### 8. 不足与局限

- **仍为前期演示，缺乏系统定量验证**：没有在标准 benchmark 上对比原始提示与 PromptMN 的任务成功率与一致性，也没有用户研究评估编写效率或理解准确度。
- **依赖模型本身的语境理解能力**：现阶段解释器本质上是概率性的，完全依赖模型对规范的正确解读，若模型误解指令则可能导致失败，且无法硬性保证约束被执行。
- **提示注入风险**：若系统提示与用户输入混合在同一上下文，恶意用户可插入 PromptMN 关键字篡改行为。论文虽提及输入清理是缓解措施，但未给出解决方案。
- **令牌开销**：若每次使用都需附加完整规范（附录 A），会大量增加提示长度。论文提到了微调、精简版规范或仅包含所用关键字定义等潜在缓解方案，但未做评估。
- **语法与实现工具缺失**：目前没有语法解析器、linter 或运行时执行引擎，无法在提示送入模型前检测结构错误，限制了其在生产环境中的可控性。
- **实验覆盖极端有限**：所展示的场景虽具代表性，但数量有限，未涵盖多语言、长上下文、复杂交互式智能体等极端情况。

（完）
