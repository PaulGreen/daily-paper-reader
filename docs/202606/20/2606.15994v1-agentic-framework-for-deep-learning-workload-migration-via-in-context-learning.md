---
title: Agentic Framework for Deep Learning workload migration via In-Context Learning
title_zh: 基于上下文学习的深度学习工作负载迁移智能体框架
authors: "Qiyue Liang, Steven Ingram, George Vanica, Andi Gavrilescu, Newfel Harrat, Hassan Sipra, Sethuraman Sankaran"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15994v1"
tags: ["query:prompt-learn"]
score: 9.0
evidence: 结合上下文学习（ICL）与基于执行神谕的自调试，构建ICL语境作为严格参考
tldr: "将深度学习模型从PyTorch迁移至JAX通常手动且易错，大语言模型因严格API难以自动化。本文提出自主智能体框架，结合上下文学习与执行oracle自调试，通过ICL参考和动态张量状态生成测试并循环修正。该方法在神经模块上数值等价率达91%（基线9%），且计算高效。为跨框架迁移提供了高可靠、可扩展的解决方案，已在SAM、T5等模型验证。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15994v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1669, \"height\": 694, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15994v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1723, \"height\": 292, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15994v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1790, \"height\": 467, \"label\": \"Table\"}]"
motivation: 迁移PyTorch模型到JAX的手动过程繁琐易错，LLM难以处理严格API对齐，迫切需要自动化方案。
method: 提出自主智能体框架，利用策划的ICL上下文作为风格参考，通过执行PyTorch获取真实张量状态作为oracle，并循环执行测试与自调试修正。
result: "在神经模块上数值等价率达91%，远超基线9%和指令+自调试27%，且计算开销不增加。"
conclusion: 该框架提供高可靠、可扩展的跨框架迁移蓝图，已在SAM、T5等模型验证，代码已开源。
---

## 摘要
将深度学习模型从 PyTorch 灵活的、面向对象的设计转换为 JAX 的函数式、无状态设置通常是一项手动且容易出错的任务。自动迁移具有挑战性，因为大型语言模型（LLM）难以处理严格且动态的 API 对齐，并且在精确操作中容易出错。我们提出了一个完全自主的系统，该系统结合了上下文学习（ICL）与基于 oracle 驱动的自我调试。首先，我们精心构建了一个 ICL 上下文，作为惯用 JAX 风格和测试用例生成的严格参考。其次，我们不依赖 LLM 推断数学输出，而是运行源 PyTorch 模块以获取其实际的动态张量状态。这创建了一个不可更改的执行 oracle。然后，我们使用一个自主的智能体循环，基于 oracle 数据合成测试。测试用例反复执行，并将回溯信息发送回 LLM 进行自我纠正。消融研究表明，将 ICL 参考与 oracle 基础和自我调试相结合，大大优于纯指令和基本智能体基线。这种改进不会增加过多的计算开销。我们的轻量级流程在神经模块上实现了 91% 的数值等价（相比之下，基线：9%，指令 + 自我调试：27%），为跨框架迁移提供了高度可靠、可扩展的蓝图。这已在多个最先进的模型上得到验证，包括 SAM（segment anything）、T5、Code Whisper 等，显示出高数值等价性。代码：https://github.com/AI-Hypercomputer/accelerator-agents/tree/main/MaxCode

## Abstract
Translating deep learning models from PyTorch's flexible, object-oriented design to JAX's functional, stateless setup is usually a manual and error-prone task. Automated migration is challenging because Large Language Models (LLMs) struggle with strict and dynamic API alignment and are prone to mistakes for exacting operations. We propose a fully autonomous system that combines In-Context Learning (ICL) with oracle-driven self-debugging. First, we curated an ICL context that serves as a strict reference for idiomatic JAX styling and test case generation. Second, instead of depending on the LLM to deduce mathematical outputs, we run the source PyTorch modules to get their actual dynamic tensor states. This creates an unchangeable execution oracle. We then use an autonomous agentic loop to synthesize tests based on the oracle data. The test cases are executed repeatedly, and the traceback is sent back to the LLM for self-correction. Ablations show that combining ICL references with oracle grounding and self-debugging greatly outperforms pure instructional and basic agentic baselines. This improvement does not add an excessive computational overhead. Our lightweight pipeline achieves 91% numerical equivalence (compared to baseline: 9%, instruction + self-debugging: 27%) on neural modules, providing a highly reliable, scalable blueprint for cross-framework migration. This has been validated across several state-of-the-art models including SAM (segment anything), T5, Code Whisper amongst others showing high numerical equivalency. Code: https://github.com/AI-Hypercomputer/accelerator-agents/tree/main/MaxCode

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：将深度学习模型从 PyTorch（面向对象、可变状态）迁移到 JAX（函数式、无状态、JIT 编译）是一项手动、耗时且容易出错的任务。自动化这一过程极具挑战，因为大型语言模型（LLM）难以严格对齐两个框架的 API，容易在复杂的张量运算和 hidden numerical 差异上产生幻觉。
- **整体含义**：论文提出一种完全自主的智能体框架，旨在利用 LLM 完成高可靠性的跨框架代码迁移，从而消除手动重写代码的障碍，加速模型在 TPU 等优化硬件上的部署。

### 2. 论文提出的方法论
整个工作流被划分为四个自动化阶段，核心在于将跨框架翻译视为“有根基的生成问题”，通过“结构锚定”与“执行神谕”相结合，实现数学等效性。
- **阶段 1：通过上下文学习（ICL）实现结构锚定**
  - 摒弃了检索增强生成（RAG）会引入分布式计算噪音的问题，改用精简、密集的 ICL 上下文。
  - ICL 上下文包含：源 PyTorch 模块、对应的惯用 JAX 翻译、相关的测试用例。它为 LLM 提供了语法格式化和功能实现的严格参考，抑制 API 幻觉。
- **阶段 2：动态执行与 PyTorch 神谕（Oracle）**
  - 将源 PyTorch 模块视为数学上的绝对真值。
  - 自动生成并运行 profiling 脚本，传入随机输入张量，提取前向传播中的权重、输入、输出等动态计算状态，序列化为不可更改的 `.pkl` 文件，形成“神谕”。
  - 其目的是绕过 LLM 在复杂算术上的局限，确保后续验证基于真实计算而非 LLM 推断。
- **阶段 3：智能体翻译与基于神谕的测试生成**
  - LLM 在 ICL 锚定下，将 PyTorch 模块翻译为使用 `flax.linen` 的 JAX 代码。
  - 随后，LLM 再利用神谕数据和 ICL 示例，自动生成验证测试套件（称为“银级”测试）。
  - 测试分三级：编译通过、形状正确、数值等价（采用 `numpy.allclose`，绝对容差 1e-7）。
- **阶段 4：迭代自调试与验证**
  - 若生成的 JAX 模块未通过测试，将回溯信息（stdout、堆栈跟踪、数值差异）反馈给 LLM，促使其进行修正。
  - 循环执行，直至代码通过所有基于神谕的测试。
  - 最终评估指标通过独立的人工评估套件进行衡量，以防止 LLM 通过生成简单测试进行“奖励黑客”行为。

**关键公式**：数值等价定义为
\[\max |f_{pt}(X, W_{pt}) - f_{jax}(X, W_{jax})| < \epsilon\]
其中 \(\epsilon = 1 \times 10^{-7}\)，用于包容不同编译器后端的浮点数差异。

### 3. 实验设计
- **数据集与评估场景**：
  - **Level 1（9 个数学函数）**：数据混合、早停、梯度裁剪、位置编码、移动平均、各种损失函数（dice, focal, label smoothing, tversky 等）。
  - **Level 2（11 个神经模块）**：MLP、VAE、GAN、GRU、LSTM、注意力机制、Transformer encoder、TCN、图卷积、ResNet 块等。
  - **Level 3（10 个完整仓库）**：Code Whisper、多模态 Transformer、Simple-MOE、时间序列预测、FinGPT、StyleGAN2、T5、双塔推荐模型、SAM2、DETR。Level 3 采用人工专家评估。
- **基准指标**：
  - 编译完整性（能否成功编译）
  - 形状等价性（张量维度一致）
  - 数值等价性（误差 < 1e-7）
  - Level 3 额外报告可读性（1-5 分）。
- **对比方法**（消融配置）：
  - 纯提示（Baseline）：无特殊指令、无 ICL、无调试。
  - 仅指令（Instruction Only）：详细的翻译指令，但无 ICL 与调试。
  - 指令 + 自调试（Instruction + Self-Debugging）：含反馈循环，但无神谕。
  - 完整流程（Full Pipeline, Ours）：指令 + ICL + 神谕驱动的自调试。

### 4. 资源与算力
- 论文**未明确说明**所使用 LLM 的具体型号、GPU 数量、训练时长或推理时的算力开销。仅提到该流程是“轻量级的”，且未产生过高的计算开销。可以推断该工作主要依赖对现有 LLM 的 API 调用进行代码生成与调试，不涉及模型训练。

### 5. 实验数量与充分性
- **实验组数**：围绕 3 个难度等级，对比了 4 种消融配置，每种配置在 Level 1 和 Level 2 上均报告了编译、形状、数值三项指标，合计 4×2×3=24 个主要数据点。Level 3 单独在 10 个仓库上评估了完整流程，提供了完整性、数值等价率、可读性。
- **充分性与公平性**：
  - 消融设计清晰地分离了指令、ICL 和神谕自调试的贡献，体现了因果推断的思路。
  - 将 ICL 中使用的模块明确排除在评估集之外，并最终通过人工评估套件衡量指标，有效防止了数据污染与奖励黑客。
  - Level 3 的评估由与开发者独立的人类专家完成，并允许最多 1-2 处微调，增强了结果的可信度。
  - 实验可重复性方面，论文附有开源代码链接。

### 6. 论文的主要结论与发现
- 纯提示或仅添加指令的 LLM 在跨框架翻译中表现极差，尤其在复杂神经模块上（Level 2 数值等价率仅为 9%～18%）。
- 加入编译驱动的自调试能提升可编译率，但缺乏数学真值约束会导致 LLM 产生“奖励黑客”行为（例如为了编译通过而牺牲数值正确性）。
- **将 ICL 结构锚定与 PyTorch 神谕相结合的执行驱动自调试**，是达到高精度的关键：Level 1 数值等价率达 100%，Level 2 达 91%，远超其他基线。
- 该自主流程提供了一种无需手动编写测试、可扩展的跨框架迁移蓝图。

### 7. 优点
- **创新的方法论**：首次将上下文学习、动态执行神谕、智能体自调试融合在一起，解决框架翻译问题。
- **严格消融实验**：充分验证了每个组件（指令、ICL、神谕驱动调试）的增量贡献。
- **科学严谨的验证**：通过独立的评估套件和人工评判，并排除 ICL 数据泄漏，保证了指标的真实性。
- **实用性强**：流程自动化程度高，从模块级到仓库级均有验证，并公开了代码实现。

### 8. 不足与局限
- **仓库级迁移尚未完全自主化**：Level 3 任务中依赖人类专家介入微小修改（1-2 处），且部分复杂仓库（如 DETR）数值等价率仅 57%，说明系统在依赖处理和大规模项目上的鲁棒性仍有待提升。
- **未探究 ICL 示例敏感性**：论文承认未系统评估不同 ICL 示例对迁移性能的影响，可能影响方法的最优性。
- **框架特异性**：当前方案仅针对 PyTorch 到 JAX 的翻译，对其他框架组合的泛化能力未经测试。
- **算力信息缺失**：未报告推理成本、单次迁移耗时等，使得“轻量级”这一论点缺少量化支撑。
- **依赖 LLM 能力边界**：论文未明确指出使用了哪个 LLM，其结论可能受限于特定模型的代码生成能力，切换到其他 LLM 时性能未必一致。

（完）
