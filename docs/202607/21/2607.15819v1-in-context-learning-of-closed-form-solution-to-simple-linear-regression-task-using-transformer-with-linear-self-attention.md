---
title: In-context learning of closed form solution to simple linear regression task using transformer with linear self-attention
title_zh: 使用线性自注意力的Transformer在上下文中学习简单线性回归任务的闭式解
authors: Katsuyuki Hagiwara
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15819v1"
tags: ["query:prompt-learn"]
score: 9.0
evidence: 使用Transformer在上下文中学习线性回归的闭式解
tldr: 变压器上下文学习通常被认为实现梯度下降，但机制是否真实获得仍存疑。本文构建线性自注意力变压器，利用层归一化在简单回归任务中近似得到最小二乘闭式解，而非梯度下降近似解。实验表明，当目标为最小二乘估计且加入L1正则化时，训练后的模型主要采用此解析实现，揭示了变压器可学习解析求解方式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15819v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 485, \"height\": 1124, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15819v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 826, \"height\": 583, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15819v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 950, \"height\": 488, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15819v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 623, \"height\": 511, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15819v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 768, \"height\": 153, \"label\": \"Table\"}]"
motivation: 现有研究多表明变压器通过梯度下降实现回归求解，但此类实现是否真正从训练中获得尚不清楚。
method: 构建具有线性自注意力的变压器，并利用层归一化机制逼近简单线性回归任务的最小二乘闭式解。
result: 实验显示，在目标为最小二乘估计且训练使用L1正则化时，模型主要采用了所构造的解析解实现。
conclusion: 变压器可通过上下文学习直接获取解析解，而非仅依赖梯度下降近似，为理解其内部机制提供新视角。
---

## 摘要
上下文学习是Transformer的一个显著特性，近年来引起了广泛关注。在许多关于上下文学习的研究中，已经表明Transformer能够实现线性和非线性回归问题的求解器，其中大多数实现了梯度下降算法。然而，这些实现是否确实通过训练获得仍不清楚。在本文中，我们构建了一个具有线性自注意力的Transformer，它在简单的回归任务中通过上下文学习最小二乘估计。此处的关键在于，通过使用层归一化来近似获得闭式（解析）解，而非基于梯度下降算法的近似解。随后，我们展示了一个实验示例，其中当目标输出为最小二乘估计时，我们的实现主要用于经过L1正则化训练的Transformer中。

## Abstract
In-context learning is a remarkable property of transformers and has recently received a lot of interest. In many studies of in-context learning, it has been shown that transformers are capable of implementing solver for linear and non-linear regression problems, in which the most of them implement gradient descent algorithm. However, it is still unclear whether those implementations have actually been acquired through training. In this paper, we construct a transformer with linear self-attention, which in-context learns the least squares estimate in a simple regression task. The point here is that the closed form (analytical) solution is approximately obtained by using layer normalization rather than an approximate solution based on gradient descent algorithm. Then, we show an experimental example, in which our implementation is mainly used in the transformer trained with l1 regularization when the target output is the least squares estimate.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

*   **核心问题**：探究Transformer在上下文学习（In-Context Learning, ICL）中解决回归任务的内在机制。现有研究普遍认为，模型通过注意力机制隐式地执行梯度下降算法来逼近解。本文挑战了这一主流观点，核心问题是：**Transformer在ICL中是否真的只能学到梯度下降，还是能学到更直接的解析解法？**
*   **整体含义**：研究者构建了一个极简的Transformer模型，证明了它可以通过上下文学习，利用**层归一化（Layer Normalization）** 来近似实现“除以方差”这个运算，从而直接得到线性回归的**闭式解（解析解）**，而非通过多步梯度下降。并通过实验表明，当训练信号很强时（直接以最小二乘估计为目标，并施加L1正则化），训练出的模型确实倾向于使用这种解析解法。这揭示了ICL的学习机制可能比预想的更灵活，能够获取更高效的算法。

### 2. 论文提出的方法论

*   **核心思想**：通过精心设计模型的权重，使Transformer的内部计算恰好映射到计算线性回归最小二乘估计的数学步骤。
*   **关键技术细节**：
    *   **模型架构**：构建了一个极小的Transformer，命名为 `TF(4, 2, 2)`，即模型维度 `D=4`，层数 `L=2`，头数 `K=2`。模型结构包括输入线性变换、两个Transformer块（含多头线性自注意力MHLSA和层归一化）、展平层和输出线性变换。
    *   **算法流程与数学原理**：
        1.  **输入嵌入**：将提示（Prompt）中的 `(1, x, y)` 映射到4维空间，其中引入了大常数 `R` 来缩放 `x` 和 `y`。
        2.  **第一层（计算均值和中心化）**：利用线性自注意力机制计算输入 `x` 和 `y` 的均值 `x̄` 和 `ȳ`。通过残差连接和精心设计的权重矩阵，第一层的输出为中心化后的变量 `Δx = x_i - x̄` 和 `Δy = y_i - ȳ`。
        3.  **第二层（计算方差、协方差并相除）**：
            *   **第一个头**：计算 `Δx` 和 `Δy` 的内积，得到协方差 `C_m` 的项。
            *   **第二个头**：计算 `Δx` 自身的内积，得到方差 `V_m` 的项。
            *   **关键步骤：通过层归一化实现除法**。第二层MHLSA的输出向量中包含了代表 `V_m` 和 `C_m` 的分量。层归一化的标准差 `σ ≈ |V_m|/√2`。归一化操作使得输出向量中产生 `C_m / V_m` 这一项，即回归系数 `θ`。
        4.  **输出提取**：最终，模型的输出层通过提取 `(N+1, 3)` 位置的值，并乘以 `-RN`，恰好得到最小二乘预测公式 `ŷ = ȳ + (C_m / V_m) * (u - x̄)`。

### 3. 实验设计

*   **数据集/场景**：合成数据集。每个任务（Prompt）的输入 `x` 和基础参数 `(θ0, θ1)` 均从标准正态分布 `N(0,1)` 中采样，噪声项 `ε` 从 `N(0, 0.04)` 采样。每个Prompt包含 `N=50` 个上下文样本和一个查询点。
*   **任务目标（Benchmark）**：模型的直接目标（教师信号）**不是**查询点的带噪声真实值，而是**数学上精确的最小二乘估计值**。这是一种更强的监督信号，直接指示模型去学习计算最小二乘解。
*   **对比方法**：本文的核心贡献是**构造**了一种解析解法，而非直接与多种算法进行性能对比。实验部分主要对比了：
    *   **不同随机初始化模型**：进行10次独立训练，分析其中哪些模型学到了所构造的机制。
    *   **模型内部机制与理想行为**：例如，分析层归一化的标准差与输入数据方差倒数的相关性。

### 4. 资源与算力

*   **文中未明确提及**任何关于GPU型号、数量或具体训练时长的信息。仅给出了训练超参数（如数据集大小 `M=5000`，最大训练轮次 `20000`，批次大小 `500`，学习率 `0.005`）。

### 5. 实验数量与充分性

*   **实验数量**：
    *   主要进行了 **10次** 独立的训练运行，使用不同的随机种子（影响数据生成和模型初始化）。
    *   对每次运行的模型，在1000个新生成的测试样本上评估性能，并计算了内部表征（层归一化标准差）与理想量（输入方差倒数）的相关性。
    *   针对其中一个“成功”的模型，进行了更深入的分析：权重矩阵可视化、对内部单元进行回归分析、以及对预测曲线的可视化。
*   **充分性与客观性**：
    *   **充分性**：对于验证“模型**能够**学到”这个特定解析解构造的论点，实验设计是充分且有针对性的。它清晰地展示了存在一个高相关性的案例，并通过一系列分析证明了该案例确实在按照预期的方式运行。
    *   **客观性**：实验较为客观。它并不声称所有模型都学到此机制（10次中仅2次出现高相关性），反而承认了模型可能学到不同的解决方案（如第4次运行相关性低但误差小），这增加了分析的客观性。然而，实验的普适性不足，场景极其简化。

### 6. 论文的主要结论与发现

*   **主要结论**：Transformer不必然通过梯度下降来进行上下文学习。它们有能力学习并实现更直接、更高效的**解析求解算法**。
*   **核心发现**：
    1.  成功构造了一个`TF(4,2,2)`小模型，利用**层归一化中的除法操作**，实现了线性回归的闭式解。
    2.  实验证实，当目标明确（最小二乘解）且施加L1正则化诱导稀疏性时，训练出的Transformer**确实可以主要依赖这种解析解法**来工作。
    3.  训练得到的模型可能存在多种求解机制，学习结果会收敛到不同但有效的方案（例如，不同的内部表征方式或计算路径）。

### 7. 优点

*   **机制新颖性**：挑战了ICL中“梯度下降说”的主流认知，提出了“解析解实现”的新视角，为理解大型语言模型内部运作提供了重要启示。
*   **方法论精妙**：对模型权重的构造性证明非常巧妙，特别是利用层归一化来实现“除法”这一在注意力机制中不易实现的操作，展现了深刻的洞察力。
*   **模型可解释性强**：通过极简的模型（`TF(4,2,2)`），使得内部计算过程可以被精确推导和验证，对具体权重和中间表征的分析令人信服。

### 8. 不足与局限

*   **场景极其简化**：仅在简单的单变量线性回归任务上验证，且输入数据分布也较简单。其能否推广到多元回归、非线性任务或更复杂的真实数据上，仍是未知数。
*   **依赖强假设和工程技巧**：
    *   模型构造严重依赖 `N`（样本数）和 `R`（一个手动设定的大常数）足够大的假设，以忽略某些计算项的边际影响。
    *   预知正确答案（解析解）来设计模型架构和权重，这与从零开始训练学习的实际场景有差距。
*   **实验覆盖有限**：
    *   未与其他ICL机制（如明确的梯度下降实现）进行性能或效率上的系统对比。
    *   仅分析了目标为“数学解”这种强力监督下的训练结果，未探索当目标为“带噪声的样本点”（更自然的场景）时，模型是否仍会学到解析解。
    *   论文未提供任何关于计算资源的信息。
*   **结论普适性存疑**：实验表明10次训练中仅少数模型学到了构造的机制，这暗示该学习过程可能对初始化或超参数敏感，并非Transformer学习ICL的唯一或必然路径。

（完）
