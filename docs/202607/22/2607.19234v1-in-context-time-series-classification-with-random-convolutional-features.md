---
title: In-Context Time Series Classification with Random Convolutional Features
title_zh: 基于随机卷积特征的上下文时间序列分类
authors: "Joscha Cüppers, Jilles Vreeken"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.19234v1"
tags: ["query:prompt-learn"]
score: 8.0
evidence: 使用预训练表格基础模型进行上下文时间序列分类
tldr: 时间序列分类任务中，随机卷积特征提取后再用简单线性分类器是常见做法，但未能充分利用特征表征。MASHT方法融合MultiRocket和Hydra特征，借助预训练表格基础模型的上下文学习能力，完全免去任务特定训练。在单变量数据集上，该方法平均排名超越HIVE-COTE 2.0；在多变量数据集上同样表现出色。这一工作验证了表格基础模型在时间序列分析中应用的可行性与有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19234v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1817, \"height\": 404, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19234v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 810, \"height\": 492, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19234v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1828, \"height\": 365, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19234v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 809, \"height\": 484, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19234v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 897, \"height\": 461, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19234v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 889, \"height\": 421, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19234v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 558, \"height\": 778, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19234v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 875, \"height\": 380, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19234v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 884, \"height\": 432, \"label\": \"Table\"}]"
motivation: 传统随机卷积特征搭配线性分类器性能受限，预训练表格基础模型可能提供更强的上下文利用能力。
method: 提出MASHT，融合MultiRocket和Hydra特征，利用预训练表格基础模型进行免训练的上下文分类。
result: 在单变量数据集上，MASHT平均排名优于HIVE-COTE 2.0；多变量数据集上与最强基线持平。
conclusion: MASHT验证了表格基础模型在时间序列分类中的有效性，为免训练的时间序列分析提供新范式。
---

## 摘要
时间序列分类在医学信号分析、工业监控和基于传感器的活动识别等领域至关重要，其中类别信息表现为局部形状、特定频率、时间偏移或复杂的跨通道交互。随机卷积变换能高效地将这些序列映射为固定维度的表格特征，但通常与简单的线性分类器结合使用。我们探讨预训练的表格基础模型是否能更有效地利用这些丰富的表示。我们提出MASHT，一个将MultiRocket和Hydra特征与上下文表格基础模型能力相结合的流程。通过利用预训练的表格基础模型，我们的方法完全绕过了任务特定的模型训练，仅需要特征提取和直接推理。大量实验表明，MASHT在单变量任务上与最先进的时间序列分类基线性能相当，其平均排名低于HIVE-COTE 2.0。在多变量数据集上，MASHT仍极具竞争力，可与最强的参考方法相媲美。

## Abstract
Time series classification is central to domains like medical signal analysis, industrial monitoring, and sensor-based activity recognition, where class information manifests as localized shapes, specific frequencies, temporal shifts, or complex cross-channel interactions. Random convolutional transforms efficiently map these sequences to fixed-dimensional tabular features but are traditionally paired with simple linear classifiers. We investigate whether a pretrained tabular foundation model can more effectively harness these rich representations.   We propose MASHT, a pipeline that marries MultiRocket and Hydra features with the power of in-context tabular foundation models. By leveraging a pretrained tabular foundation model, our approach completely bypasses task-specific model training, requiring only feature extraction and direct inference. Extensive experiments demonstrate that MASHT matches state-of-the-art time series classification baselines on univariate tasks, achieving a lower average rank than HIVE-COTE 2.0. On multivariate datasets, MASHT remains highly competitive with the strongest reference methods.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究背景**：时间序列分类（TSC）在医疗、工业监控等领域应用广泛，其核心挑战在于类别信息可能表现为局部形状、频率、时序偏移或通道交互等多种形式，需要强大的表征能力。现有高效方法（如ROCKET系列）通常使用随机卷积核提取特征，但下游搭配的是简单的线性分类器（如岭回归），这限制了它们捕获特征间非线性交互的能力。
- **核心问题**：能否利用一种预训练好的、通用的表格数据基础模型作为下游分类器，来更有效地利用随机卷积核提取的丰富时间序列特征，从而在无需任务特定训练的情况下，实现高精度的时序分类？
- **整体含义**：该论文探索了“专用时序特征提取器 + 通用表格基础模型”这一模块化范式的潜力，旨在结合两者的优势：用成熟的时序变换编码时间结构，用强大的表格模型进行上下文推理，为高效、免训练的时间序列分类提供新思路。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：提出名为 **MASHT** 的两阶段流程。第一阶段，使用固定的随机卷积变换器（MultiRocket和Hydra）将每个时序样本转化为一个固定维度的特征向量；第二阶段，将这些特征向量构造成一个表格数据集，直接输入预训练的表格基础模型 **TabPFN-3** 进行上下文推理，完全跳过针对特定任务的模型训练。
- **关键技术细节与流程**：
  - **问题设定**：给定带标签的训练集 `Dtrain = {(x_i, y_i)}`，目标是为未知序列 `x*` 预测类别概率 `p(y | x*, Dtrain)`。
  - **特征提取（第一阶段）**：
    - **MultiRocket** 表示 `ϕMR(x)`：对原始序列和一阶差分序列应用大量随机卷积核，并通过多种池化算子（如正值的比例、最大值等）汇总核响应。
    - **Hydra** 表示 `ϕH(x)`：将卷积核分组，组内竞争，统计每个核获得极端（最大/最小）响应的次数，形成类似字典计数的特征。
    - **特征拼接**：将上述两种特征拼接为一个长向量 `z(x) = [ϕMR(x); ϕH(x)]`，构成变换后的训练集 `Dz`。
  - **特征预算管理**：为防止特征维度过大，根据数据集样本总数设定特征预算（如样本少于1000，预算为10,000维），并将其均分给MultiRocket和Hydra。Hydra会根据序列长度自适应调整参数，以落在预算内。
  - **上下文预测（第二阶段）**：
    - **分类器**：使用 `fTabPFN-3`，一个在大量合成表格任务上预训练的Transformer模型。
    - **推理**：对于查询样本 `x*`，提取其特征 `z(x*)`，连同变换后的训练集 `Dz` 一起输入TabPFN-3，模型基于上下文直接输出类别预测，其过程可形式化为 `p̂(y | x*) = fTabPFN-3(z(x*); Dz)`。

### 3. 实验设计：使用了哪些数据集/场景，它的benchmark是什么，对比了哪些方法
- **数据集与场景**：
  - **单变量时序分类**：使用 **UTF-112 基准**，包含112个单变量时间序列数据集，每个数据集有30次重采样划分。
  - **多变量时序分类**：使用 **Multiverse 基准**，包含71个多变量时间序列数据集。
- **对比方法**：
  - **单变量实验对比**：HIVE-COTE 2.0, MR-Hydra, QUANT, RDST, WEASEL-2, FreshPRINCE, Proximity Forest, H-InceptionTime。
  - **多变量实验对比**：HIVE-COTE 2.0, MR-Hydra, RDST, Arsenal, FreshPRINCE, ROCKET, QUANT, CIF, DrCIF, RIST, STC, TDE, Catch22, InceptionTime, LITETime, 1NN-DTW。
- **评估指标**：
  - 主要指标：准确率。
  - 辅助指标：平衡准确率、AUROC（仅UTF-112）。
  - 统计检验：使用平均排名（mean rank）、两两比较采用Holm校正的Wilcoxon符号秩检验，并通过临界差异图展示。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）
- **硬件配置**：所有实验均在 **一台配备40GB显存的NVIDIA A100 GPU** 上运行。
- **推理耗时**：论文提供了端到端推理时间的统计，但**未报告任何训练时长**（因为该方法的核心是无须任务特定训练的）。在UTF-112数据集上，MASHT处理单个数据集的中位总耗时为**59.7秒**，其中绝大部分时间用于分类（中位59.5秒），特征提取非常快（Hydra中位0.46秒，MultiRocket中位0.06秒）。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平
- **实验数量**：论文对 **112个单变量**和 **71个多变量** 数据集进行了完整评估，总计覆盖了 **183个数据集**。对于UTF-112，还在30次重采样上运行，实验量充足。
- **客观性与公平性**：
  - 实验设计客观公平。论文严格遵循了对应基准测试（UTF-112和Multiverse）的官方评估协议，直接使用了报告中的基线方法结果，确保了比较的公平性。
  - 统计分析方法严谨，使用了平均排名、Holm校正的假设检验，并展示了临界差异图和散点图，从多个维度揭示性能差异。
- **实验充分性评估**：
  - **充分之处**：覆盖了单变量和多变量两大主流场景，对比了包括最强集成方法（HIVE-COTE 2.0）在内的多种代表性方法。对关键比较对象MR-Hydra的分析，清晰地剥离了“特征提取器”和“下游分类器”的贡献。
  - **不足之处**：论文未包含消融实验，例如，没有单独分析MultiRocket或Hydra特征与TabPFN结合的效果，也没有分析不同特征预算对性能的影响。同时，缺乏对模型在不同序列长度、类别数量等数据特性下性能的系统性分析。

### 6. 论文的主要结论与发现
- **单变量性能领先**：在UTF-112上，MASHT取得了最高的平均准确率、平衡准确率和AUROC，其平均排名低于所有基线方法（包括HIVE-COTE 2.0），但并未在统计上显著优于HIVE-COTE 2.0。
- **显著优于同源方法**：MASHT相较于使用相同特征但采用线性分类器的MR-Hydra，取得了统计显著的优势，这表明强大的下游表格模型确实能更有效地利用随机卷积特征。
- **多变量性能持平**：在Multiverse上，MASHT虽具竞争力，但与HIVE-COTE 2.0、MR-Hydra等最强方法没有显著差异，并未建立新的最优水平。
- **性能差异的启示**：单变量和多变量结果的对比表明，将时序压缩为固定表格表示再分类的范式在单变量场景下尤其有效；而在需要捕捉复杂跨通道依赖的多变量场景中，HIVE-COTE 2.0这类集成多种时序表征的方法可能更具优势。

### 7. 优点：方法或实验设计上有哪些亮点
- **范式创新**：首次成功将预训练的表格基础模型（TabPFN-3）应用于时间序列分类，验证了“时序特征提取 + 表格模型推理”这一模块化、免训练范式的有效性和竞争力。
- **方法简洁高效**：流程清晰，完全无需针对下游任务进行任何形式的模型训练、微调或超参数搜索，大幅简化了分类流程。
- **实验论证扎实**：在两大权威基准（UTF-112, Multiverse）上严格遵循标准协议进行评估，与全面的基线方法进行比较，并通过严谨的统计假设检验支撑结论，说服力强。
- **与特征级对比**：通过直接对比MR-Hydra，巧妙地证明了性能提升主要归功于下游分类器的升级（从线性模型到TabPFN-3），而非特征本身，观点清晰。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **缺乏消融与深入分析**：论文未对特征预算、不同特征组合（仅MultiRocket或Hydra）的影响、序列长度与性能的关系等关键因素进行消融研究和深入分析，留下了许多未解释的问题。
- **算力与速度缺陷**：尽管免训练，但TabPFN-3的推理需要高端GPU（A100）的支持，且分类耗时（>40s）远超传统的线性分类器，单变量任务上的总时间也长于某些高效方法。与HC2的纯CPU训练时间对比维度不一致，缺乏严格控制的效率对比。
- **应用场景受限**：在更复杂、需要捕捉丰富跨通道交互或长程依赖的多变量任务上，该方法的优势不明显，表明其固定表格表示的范式存在能力瓶颈。
- **模型可解释性差**：TabPFN-3作为“黑箱”模型，使得整个流程失去了可解释性，无法像简单线性模型或基于形状的方法那样提供决策依据。
- **结论的泛化性**：所有结论仅限于所使用的两个基准测试，其在更大规模、更多样化实际应用中的表现尚待验证。

（完）
